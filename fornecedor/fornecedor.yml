spring:
    datasource:
        password: root
        url: jdbc:mysql://localhost:3306/fornecedor
        username: root
    jpa:
        hibernate:
            ddl-auto: create
        show-sql: true
